image:
  repository: defendersd/defender.user-management
  tag: 20260715-211
  pullPolicy: Always
ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: argo-user-management
      paths:
        - path: /
          pathType: Prefix
autoscaling:
  # Called synchronously from Portal and Identity over cluster DNS.
  minReplicaCount: 1
  http:
    enabled: true
