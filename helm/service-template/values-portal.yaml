image:
  repository: defendersd/defender.portal
  tag: sha-c0c0dbd
  pullPolicy: Always
ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: argo-portal
      paths:
        - path: /
          pathType: Prefix
autoscaling:
  http:
    enabled: true
