image:
  repository: defendersd/defender.portal
  tag: sha-d689d29
  pullPolicy: Always
ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: argo-portal
      paths:
        - path: /
          pathType: Prefix
autoscaling:
  http:
    enabled: true
