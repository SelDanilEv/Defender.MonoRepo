image:
  repository: defendersd/defender.identity
  tag: latest
  pullPolicy: Always

ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: argo-identity
      paths:
        - path: /
          pathType: Prefix
