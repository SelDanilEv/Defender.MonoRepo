image:
  repository: defendersd/defender.job-scheduler
  tag: sha-6cb25b4
  pullPolicy: Always
ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: argo-job-scheduler
      paths:
        - path: /
          pathType: Prefix
autoscaling:
  http:
    enabled: true
  cron:
    enabled: true
