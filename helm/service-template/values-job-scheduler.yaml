image:
  repository: defendersd/defender.job-scheduler
  tag: sha-1cc8683
  pullPolicy: Always
ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: argo-job-scheduler
      paths:
        - path: /
          pathType: Prefix
autoscaling:
  http:
    enabled: true
  cron:
    enabled: true
