image:
  repository: defendersd/defender.notification
  tag: sha-fc52fda
  pullPolicy: Always
ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: argo-notification
      paths:
        - path: /
          pathType: Prefix
autoscaling:
  # Identity calls notification directly over cluster DNS.
  minReplicaCount: 1
  http:
    enabled: true
