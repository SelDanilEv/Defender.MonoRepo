image:
  repository: defendersd/defender.travel-calendar
  tag: 20260819-324
  digest: sha256:d12f2267a65d425e3346ed4c6d6fe71801e4db8a7bfdc1fc534fcf2cd0d18013
  pullPolicy: Always
deploymentStrategy:
  # The single-node cluster cannot schedule the old and replacement pods together.
  type: RollingUpdate
  rollingUpdate:
    maxSurge: 0
    maxUnavailable: 1
resources:
  # Keep the request schedulable while retaining the shared service limit.
  requests:
    cpu: "5m"
ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: argo-travel-calendar
      paths:
        - path: /
          pathType: Prefix
autoscaling:
  # Portal calls travel-calendar directly over cluster DNS.
  minReplicaCount: 1
  http:
    enabled: true
