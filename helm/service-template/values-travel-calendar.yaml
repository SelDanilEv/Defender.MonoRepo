image:
  repository: defendersd/defender.travel-calendar
  tag: sha-064dc6d
  # Docker Hub latest manifest, resolved 2026-07-11.
  digest: sha256:4386e5a3a5144b4361fd8b37aedb847721f61636d735f93ea96194fc8c079da5
  pullPolicy: Always
ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: argo-travel-calendar
      paths:
        - path: /
          pathType: Prefix
autoscaling:
  # Portal calls travel-calendar directly over cluster DNS.
  minReplicaCount: 1
  http:
    enabled: true
probes:
  # TravelCalendarService exposes its unauthenticated health endpoint at /health.
  path: /health
