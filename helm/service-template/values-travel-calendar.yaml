image:
  repository: defendersd/defender.travel-calendar
  tag: latest
  digest: sha256:3d4ea3ac74810916d34e2340f4176090ec0f3ba65411032dc8ec38a939f6e580
  pullPolicy: Always
deploymentStrategy:
  # The single-node cluster cannot schedule the old and replacement pods together.
  type: RollingUpdate
  rollingUpdate:
    maxSurge: 0
    maxUnavailable: 1
resources:
  # Keep the request schedulable while retaining the shared service limit.
  requests:
    cpu: "5m"
ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: argo-travel-calendar
      paths:
        - path: /
          pathType: Prefix
autoscaling:
  # Portal calls travel-calendar directly over cluster DNS.
  minReplicaCount: 1
  http:
    enabled: true
