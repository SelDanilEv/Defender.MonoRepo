image:
  repository: defendersd/defender.budget-tracker
  tag: abdce20b8aa32067145985e8cf6003907dbc1a1d
  pullPolicy: Always
ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: argo-budget-tracker
      paths:
        - path: /
          pathType: Prefix
autoscaling:
  # Portal calls budget-tracker directly over cluster DNS.
  minReplicaCount: 1
  http:
    enabled: true
