global:
  environment: dev

kube-prometheus-stack:
  grafana:
    ingress:
      enabled: false
  prometheus:
    prometheusSpec:
      retention: 7d

loki:
  singleBinary:
    persistence:
      enabled: true
      size: 10Gi

promtail:
  tolerations: []
